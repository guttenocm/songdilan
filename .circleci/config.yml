version: 2.1

# Define the jobs we want to run for this project
jobs:
  gas1:
    docker:
      - image: cimg/base:stable
    steps:
      - run: sudo apt-get update && sudo apt-get install iputils-ping
  gas2:
    docker:
      - image: cimg/base:stable
    steps:
      - run: sudo apt-get update && sudo apt-get install iputils-ping
  gas3:
    docker:
      - image: cimg/base:stable
    steps:
      - run: sudo apt-get update && sudo apt-get install iputils-ping
  gas4:
    docker:
      - image: cimg/base:stable
    steps:
      - run: sudo apt-get update && sudo apt-get install iputils-ping

# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - gas1
      - gas2
      - gas3
      - gas4