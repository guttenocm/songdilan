version: 2.1

# Define the jobs we want to run for this project
jobs:
  gas1:
    docker:
      - image: cimg/base:stable
    steps:
      - run: sudo apt-get update && sudo apt-get install iputils-ping && which ping && ls -l /usr/bin/

# Orchestrate our job run sequence
workflows:
  build_and_test:
    jobs:
      - gas1